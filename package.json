{
  "name": "video-wall-browser",
  "version": "1.0.0",
  "description": "Proxy Tester + Video Wall + Bot Detection Test",
  "main": "merged-server.js",
  "scripts": {
    "start": "node merged-server.js",
    "build": "npm install && npx playwright install chromium",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "node-fetch": "^2.7.0",
    "https-proxy-agent": "^7.0.4",
    "socks-proxy-agent": "^8.0.3",
    "playwright": "^1.41.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
