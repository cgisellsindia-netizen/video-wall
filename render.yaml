services:
  - type: web
    name: video-wall-browser
    runtime: node
    buildCommand: npm install && npx playwright install chromium
    startCommand: node merged-server.js
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: 0
    plan: free
